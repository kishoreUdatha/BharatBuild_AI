# ============================================
# CI test requirements
# ============================================
# Previously this file hand-listed a SUBSET of the runtime deps, which meant CI
# tested against a dependency set that never shipped (missing asyncpg,
# psycopg2-binary, google-genai, resend, and pinning pydantic==2.5.3 while prod
# shipped >=2.9.0). It now includes the real runtime set so tests exercise the
# same packages production runs.

-r requirements.txt

# CI-only: SQLite async driver, used in place of Postgres for unit tests
aiosqlite>=0.19.0
