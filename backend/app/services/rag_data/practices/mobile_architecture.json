[
  {
    "content": "# React Native Application Architecture\n\n## Recommended Folder Structure\n```\nsrc/\n├── assets/                  # Images, fonts\n├── components/\n│   ├── ui/                  # Reusable UI\n│   │   ├── Button.tsx\n│   │   └── Input.tsx\n│   └── features/            # Feature components\n├── hooks/                   # Custom hooks\n│   ├── useAuth.ts\n│   └── useFetch.ts\n├── navigation/              # React Navigation\n│   ├── AppNavigator.tsx\n│   ├── AuthNavigator.tsx\n│   └── types.ts\n├── screens/                 # Screen components\n│   ├── auth/\n│   │   ├── LoginScreen.tsx\n│   │   └── RegisterScreen.tsx\n│   └── main/\n│       └── HomeScreen.tsx\n├── services/                # API services\n│   └── api.ts\n├── store/                   # State management\n│   ├── authStore.ts\n│   └── appStore.ts\n├── theme/                   # Styling\n│   ├── colors.ts\n│   └── spacing.ts\n├── types/                   # TypeScript types\n└── utils/                   # Utilities\n```\n\n## Navigation Pattern\n```typescript\n// navigation/AppNavigator.tsx\nimport { NavigationContainer } from '@react-navigation/native';\nimport { createNativeStackNavigator } from '@react-navigation/native-stack';\n\nconst Stack = createNativeStackNavigator<RootStackParamList>();\n\nexport function AppNavigator() {\n  const { user } = useAuth();\n  \n  return (\n    <NavigationContainer>\n      <Stack.Navigator>\n        {user ? (\n          <Stack.Screen name=\"Main\" component={MainNavigator} />\n        ) : (\n          <Stack.Screen name=\"Auth\" component={AuthNavigator} />\n        )}\n      </Stack.Navigator>\n    </NavigationContainer>\n  );\n}\n```\n\n## State Management with Zustand\n```typescript\nimport { create } from 'zustand';\nimport { persist, createJSONStorage } from 'zustand/middleware';\nimport AsyncStorage from '@react-native-async-storage/async-storage';\n\nexport const useAppStore = create(\n  persist(\n    (set) => ({\n      theme: 'light',\n      setTheme: (theme) => set({ theme }),\n    }),\n    {\n      name: 'app-storage',\n      storage: createJSONStorage(() => AsyncStorage),\n    }\n  )\n);\n```",
    "metadata": {
      "title": "React Native Application Architecture",
      "framework": "react-native",
      "language": "typescript",
      "tags": ["architecture", "structure", "react-native", "navigation", "mobile"]
    }
  },
  {
    "content": "# React Native Debugging Guide\n\n## Metro Bundler Issues\n\n### Problem\n`Unable to resolve module` or bundle errors.\n\n### Solutions\n```bash\n# Clear cache and restart\nnpx react-native start --reset-cache\n\n# Clear watchman\nwatchman watch-del-all\n\n# Clean and reinstall\nrm -rf node_modules\nrm -rf ios/Pods\nnpm install\ncd ios && pod install && cd ..\n\n# For Android\ncd android && ./gradlew clean && cd ..\n```\n\n## Performance Issues\n\n```typescript\n// BAD: Inline functions cause re-renders\n<FlatList\n  data={items}\n  renderItem={({ item }) => <Item item={item} />}  // New fn each render!\n/>\n\n// GOOD: Stable function reference\nconst renderItem = useCallback(\n  ({ item }) => <Item item={item} />,\n  []\n);\n<FlatList data={items} renderItem={renderItem} />\n\n// BAD: Missing keys\n{items.map(item => <Item item={item} />)}  // No key!\n\n// GOOD: Always use keys\n{items.map(item => <Item key={item.id} item={item} />)}\n\n// Optimize FlatList\n<FlatList\n  data={items}\n  keyExtractor={(item) => item.id}\n  renderItem={renderItem}\n  removeClippedSubviews={true}\n  maxToRenderPerBatch={10}\n  windowSize={5}\n  getItemLayout={(data, index) => ({\n    length: ITEM_HEIGHT,\n    offset: ITEM_HEIGHT * index,\n    index,\n  })}\n/>\n```\n\n## Native Module Errors\n\n```bash\n# iOS: Pod not linked\ncd ios && pod install\n\n# Android: Module not linked (RN < 0.60)\nnpx react-native link package-name\n\n# Clean native builds\n# iOS\ncd ios && rm -rf build && pod install\n\n# Android\ncd android && ./gradlew clean\n```\n\n## Debugging Tools\n\n```typescript\n// Use Flipper for debugging\n// Or React Native Debugger\n\n// Console logging\nconsole.log('Data:', JSON.stringify(data, null, 2));\n\n// Check render count\nconst renderCount = useRef(0);\nuseEffect(() => {\n  renderCount.current++;\n  console.log('Render count:', renderCount.current);\n});\n```",
    "metadata": {
      "title": "React Native Debugging Guide",
      "framework": "react-native",
      "language": "typescript",
      "tags": ["debugging", "react-native", "performance", "metro", "mobile"]
    }
  },
  {
    "content": "# Flutter Application Architecture\n\n## Recommended Folder Structure\n```\nlib/\n├── core/                    # Core utilities\n│   ├── constants/\n│   ├── theme/\n│   ├── utils/\n│   └── errors/\n├── data/                    # Data layer\n│   ├── models/\n│   ├── repositories/\n│   └── datasources/\n│       ├── local/\n│       └── remote/\n├── domain/                  # Business logic\n│   ├── entities/\n│   ├── repositories/       # Abstract\n│   └── usecases/\n├── presentation/            # UI layer\n│   ├── providers/           # State management\n│   ├── screens/\n│   ├── widgets/\n│   └── routes/\n└── main.dart\n```\n\n## State Management with Provider\n```dart\n// providers/auth_provider.dart\nclass AuthProvider with ChangeNotifier {\n  User? _user;\n  bool _loading = false;\n\n  User? get user => _user;\n  bool get loading => _loading;\n  bool get isAuthenticated => _user != null;\n\n  Future<void> login(String email, String password) async {\n    _loading = true;\n    notifyListeners();\n    try {\n      _user = await _authRepository.login(email, password);\n    } finally {\n      _loading = false;\n      notifyListeners();\n    }\n  }\n}\n\n// main.dart\nvoid main() {\n  runApp(\n    MultiProvider(\n      providers: [\n        ChangeNotifierProvider(create: (_) => AuthProvider()),\n        ChangeNotifierProvider(create: (_) => ThemeProvider()),\n      ],\n      child: MyApp(),\n    ),\n  );\n}\n```\n\n## Clean Architecture Pattern\n```dart\n// domain/usecases/get_user.dart\nclass GetUser {\n  final UserRepository repository;\n  GetUser(this.repository);\n  \n  Future<User> call(String id) => repository.getUser(id);\n}\n\n// presentation/providers/user_provider.dart\nclass UserProvider with ChangeNotifier {\n  final GetUser getUser;\n  UserProvider(this.getUser);\n  \n  Future<void> loadUser(String id) async {\n    _user = await getUser(id);\n    notifyListeners();\n  }\n}\n```",
    "metadata": {
      "title": "Flutter Application Architecture",
      "framework": "flutter",
      "language": "dart",
      "tags": ["architecture", "structure", "flutter", "provider", "clean-architecture"]
    }
  },
  {
    "content": "# Flutter Debugging Guide\n\n## Build Errors\n\n### Problem\nGradle or Xcode build failures.\n\n### Solutions\n```bash\n# Clean Flutter\nflutter clean\nflutter pub get\n\n# iOS specific\ncd ios\nrm -rf Pods Podfile.lock\npod install --repo-update\ncd ..\n\n# Android specific\ncd android\n./gradlew clean\ncd ..\n\n# Upgrade Flutter\nflutter upgrade\nflutter doctor\n```\n\n## State Not Updating UI\n\n```dart\n// BAD: Mutating list\nclass MyProvider with ChangeNotifier {\n  List<Item> items = [];\n  \n  void addItem(Item item) {\n    items.add(item);  // Mutating!\n    notifyListeners();  // May not trigger rebuild!\n  }\n}\n\n// GOOD: Create new list\nvoid addItem(Item item) {\n  items = [...items, item];  // New list\n  notifyListeners();\n}\n\n// BAD: Not calling notifyListeners\nvoid updateUser(User user) {\n  _user = user;\n  // Forgot notifyListeners()!\n}\n\n// GOOD: Always notify\nvoid updateUser(User user) {\n  _user = user;\n  notifyListeners();\n}\n```\n\n## Widget Overflow\n\n```dart\n// BAD: Unbounded in scroll\nColumn(\n  children: [\n    Expanded(child: ListView()),  // Error in Column!\n  ],\n)\n\n// GOOD: Use Flexible or remove Expanded\nColumn(\n  children: [\n    Flexible(child: ListView()),\n  ],\n)\n\n// Or use ListView directly\nListView(\n  children: [...],\n)\n```\n\n## Performance Debugging\n\n```dart\n// Enable performance overlay\nMaterialApp(\n  showPerformanceOverlay: true,\n);\n\n// Use const widgets\nconst SizedBox(height: 16);  // Not rebuilt\n\n// Profile rebuilds\nclass MyWidget extends StatelessWidget {\n  @override\n  Widget build(BuildContext context) {\n    debugPrint('MyWidget rebuild');  // Track rebuilds\n    return ...;\n  }\n}\n\n// Use DevTools\nflutter pub global activate devtools\nflutter pub global run devtools\n```",
    "metadata": {
      "title": "Flutter Debugging Guide",
      "framework": "flutter",
      "language": "dart",
      "tags": ["debugging", "flutter", "build", "state", "performance"]
    }
  }
]
