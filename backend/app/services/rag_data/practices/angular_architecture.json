[
  {
    "content": "# Angular Application Architecture\n\n## Recommended Folder Structure\n```\nsrc/app/\n├── core/                    # Singleton services, guards\n│   ├── services/\n│   │   ├── auth.service.ts\n│   │   └── api.service.ts\n│   ├── guards/\n│   │   └── auth.guard.ts\n│   ├── interceptors/\n│   │   └── auth.interceptor.ts\n│   └── core.module.ts\n├── shared/                  # Shared components, pipes, directives\n│   ├── components/\n│   │   ├── button/\n│   │   └── modal/\n│   ├── pipes/\n│   ├── directives/\n│   └── shared.module.ts\n├── features/                # Feature modules\n│   ├── auth/\n│   │   ├── components/\n│   │   ├── services/\n│   │   └── auth.module.ts\n│   ├── dashboard/\n│   └── users/\n├── layouts/                 # Layout components\n└── app.component.ts\n```\n\n## Key Principles\n1. **Standalone Components**: Prefer standalone over NgModules\n2. **Signals**: Use signals for reactive state (Angular 16+)\n3. **Lazy Loading**: Lazy load feature modules\n4. **Smart/Dumb Components**: Container vs presentational\n5. **OnPush Change Detection**: For better performance\n\n## Modern Angular Pattern (v17+)\n```typescript\n// Standalone component with signals\n@Component({\n  selector: 'app-counter',\n  standalone: true,\n  imports: [CommonModule],\n  template: `\n    <button (click)=\"increment()\">Count: {{ count() }}</button>\n  `,\n  changeDetection: ChangeDetectionStrategy.OnPush\n})\nexport class CounterComponent {\n  count = signal(0);\n  doubled = computed(() => this.count() * 2);\n  \n  increment() {\n    this.count.update(c => c + 1);\n  }\n}\n\n// Route with lazy loading\nexport const routes: Routes = [\n  {\n    path: 'dashboard',\n    loadComponent: () => import('./dashboard/dashboard.component')\n      .then(m => m.DashboardComponent),\n    canActivate: [authGuard]\n  }\n];\n```",
    "metadata": {
      "title": "Angular Application Architecture",
      "framework": "angular",
      "language": "typescript",
      "tags": ["architecture", "structure", "angular", "standalone", "signals"]
    }
  },
  {
    "content": "# Angular Debugging Guide\n\n## Change Detection Not Triggering\n\n### Problem\nUI not updating after data changes.\n\n### Solutions\n```typescript\n// With OnPush, must use immutable updates\n@Component({\n  changeDetection: ChangeDetectionStrategy.OnPush\n})\nexport class ListComponent {\n  items = signal<Item[]>([]);\n  \n  // BAD: Mutating array\n  addItem(item: Item) {\n    this.items().push(item);  // Won't trigger!\n  }\n  \n  // GOOD: Create new array\n  addItem(item: Item) {\n    this.items.update(items => [...items, item]);\n  }\n}\n\n// Force change detection if needed\nconstructor(private cdr: ChangeDetectorRef) {}\n\nngAfterViewInit() {\n  // After async operation\n  this.cdr.detectChanges();\n}\n```\n\n## Dependency Injection Errors\n\n```typescript\n// ERROR: NullInjectorError: No provider for ServiceX\n\n// Solution 1: providedIn root\n@Injectable({ providedIn: 'root' })\nexport class AuthService {}\n\n// Solution 2: Provide in component\n@Component({\n  providers: [MyService]  // New instance per component\n})\n\n// Solution 3: Provide in route\n{\n  path: 'admin',\n  providers: [AdminService],\n  loadComponent: () => import('./admin.component')\n}\n```\n\n## Observable Memory Leaks\n\n```typescript\n// BAD: Not unsubscribing\nexport class MyComponent {\n  ngOnInit() {\n    this.http.get('/api/data').subscribe(data => {\n      this.data = data;  // Leaks if component destroyed!\n    });\n  }\n}\n\n// GOOD: Using takeUntilDestroyed (Angular 16+)\nexport class MyComponent {\n  private destroyRef = inject(DestroyRef);\n  \n  ngOnInit() {\n    this.http.get('/api/data')\n      .pipe(takeUntilDestroyed(this.destroyRef))\n      .subscribe(data => this.data = data);\n  }\n}\n\n// GOOD: Using async pipe (auto-unsubscribes)\n@Component({\n  template: `<div *ngIf=\"data$ | async as data\">{{ data.name }}</div>`\n})\nexport class MyComponent {\n  data$ = this.http.get('/api/data');\n}\n```",
    "metadata": {
      "title": "Angular Debugging Guide",
      "framework": "angular",
      "language": "typescript",
      "tags": ["debugging", "angular", "change-detection", "rxjs", "memory-leaks"]
    }
  }
]
