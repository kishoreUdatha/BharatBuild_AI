
JAVA/SPRING BOOT RULES:

🚨 NO LOMBOK - USE EXPLICIT GETTERS/SETTERS:
- DO NOT use Lombok annotations (@Data, @Getter, @Setter, @Builder, @NoArgsConstructor, @AllArgsConstructor, @RequiredArgsConstructor)
- DO NOT import lombok.*
- For EVERY field, generate explicit getter and setter methods
- Generate explicit no-arg constructor and all-args constructor
- Use constructor injection for service dependencies

PACKAGE FROM FILE PATH:
- Remove: src/main/java/ prefix
- Replace / with .
- Remove filename
- Result: package statement

IMPORTS - DERIVE FROM CONTEXT:
1. Read "FILES ALREADY CREATED" section
2. Find file containing needed class
3. Convert path to package + class name
4. Add import statement

ENUMS MUST BE SEPARATE FILES!

ENTITY CLASS REQUIREMENTS:
- @Entity and @Table annotations
- @Id and @GeneratedValue for primary key
- Private fields with JPA annotations
- Public no-arg constructor
- Public all-args constructor
- Public getter for EVERY field
- Public setter for EVERY field

DTO CLASS REQUIREMENTS:
- Private fields
- Public no-arg constructor
- Public all-args constructor
- Public getter for EVERY field
- Public setter for EVERY field

SERVICE REQUIREMENTS:
- @Service annotation
- Private final repository fields
- Constructor with dependencies (constructor injection)

CONTROLLER REQUIREMENTS:
- @RestController annotation
- @RequestMapping for base path
- Constructor with dependencies (constructor injection)

TYPE CONSISTENCY:
- Money: BigDecimal
- ID: Long or UUID
- Date: LocalDateTime
- Enum: Separate file in model/enums/

VERIFICATION:
- Package matches file path
- All imports present (NO lombok!)
- Explicit getters/setters for ALL fields
- Constructor injection (not @RequiredArgsConstructor)
