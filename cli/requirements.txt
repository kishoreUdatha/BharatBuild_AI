# BharatBuild CLI — Python dependencies
# Install:  pip install -r cli/requirements.txt

# HTTP client with SSE streaming support
httpx[http2]>=0.27.0

# Terminal UI — rendering, markdown, syntax highlighting
rich>=13.7.0

# Interactive prompt — autocomplete, history, key bindings
prompt-toolkit>=3.0.47

# Async file I/O
aiofiles>=23.2.1

# Config / env
python-dotenv>=1.0.1
pyyaml>=6.0.1

# Faster event loop (Linux / macOS only)
uvloop>=0.19.0; platform_system != "Windows"
